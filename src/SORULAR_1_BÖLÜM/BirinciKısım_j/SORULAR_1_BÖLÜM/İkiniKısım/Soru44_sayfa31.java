package SORULAR_1_BÖLÜM.BirinciKısım_j.SORULAR_1_BÖLÜM.İkiniKısım;

public class Soru44_sayfa31 {
    public static void main(String[] args) {

        //Natural Number Sum Recursion-->Doğal Sayı Toplamı Özyineleme

        //  Questions:Java program to check whether a number can be expressed as sum of two prime numbers
        //soru: Bir sayının iki asal sayının toplamı olarak ifade edilip edilemeyeceğini kontrol eden Java programı

        //34=3+31,5+29,11+23,17+17

        int num = 34;
        boolean flog = false;

        for (int i = 2; i <= num / 2; i++) {

       /*     if (checkPrimeNumbeer(i)) {
                if (checkPrimeNumbeer(num - i)) {
                    System.out.println(num + "=" + i + "+" + (num - i));
                    flog = true;
                }
            }

        }
        if (!flog) {
            System.out.println(num + "cannot be expressed as the sum of two prime numbers");
        }

    }



        */}
        }}